# REFLECTION · 反思报告

> 项目：CageHarness — Coding Agent Harness（AI4SE 期末项目 A）
> 作者：向天宇　日期：2026-08-14
> 开发配置：Claude Code（主开发 agent）+ ChatGPT（外部审阅者）+ Superpowers 工作流

## 一、哪些技能发挥了最大作用，哪些"形式大于实质"

最有价值的是 brainstorming 与 writing-plans 的迭代式追问。SPEC 从 V1 演进到 V2.2 经历四轮架构审阅，采纳 40 余条意见、推翻 3 条。其中三条是架构级的：Agent Loop 从流水线式函数串联改为状态机驱动（补上 5 种停机条件）、Memory 从全量注入改为按需检索、治理从三层升级为五层管线加审计日志。没有这些迭代，V1 的"无 State 循环"和"全量注入记忆"会带着致命缺陷进入实现。

requesting-code-review 我落实为"每 Phase 由 ChatGPT 独立 Gate Review"，发现了 HITL 审批不可达、Retriever 缺注入阈值等 Phase 级问题，全部在开发期修正。

形式大于实质的：subagent-driven 的仪式感在单人项目里打折——我的 subagent 主要用于并行搜索与评审，而非"每个 task 一个新鲜 subagent"；TDD 在脚手架阶段纯粹是走流程；finishing-a-development-branch 在单人仓库里退化为一次 merge。这些技能的价值随项目规模增长，本项目规模还不足以完全兑现它们。

## 二、TDD 强制是阻碍还是放大器

双重性，取决于被测对象能否被确定性验证。脚手架阶段是阻碍：为 go.mod、.gitignore 写红-绿测试的意义接近于零。核心机制阶段是放大器：Phase 6 治理管线先写失败测试再实现，五层管线的短路行为在测试里被精确刻画（Demo 4 断言审计记录只含 2 个阶段而非 5 个）；Phase 10 的 TaskManager 是第一个大量使用 goroutine 的模块，测试先行让我在结构上就避免了数据竞争。红-绿-重构中"先看到红色"尤其重要——它防止 subagent 和我在"似乎完成了"的幻觉上继续叠加代码。我的结论：AI 协作下 TDD 最大的价值不是防回归，而是给"完成"一个不可商量的客观定义。

## 三、subagent 能自主运行多久不偏离

经验数据：当 task 满足"2–5 分钟粒度 + 明确文件路径 + 明确验证步骤"时，一个新鲜 subagent 可自主运行 30–60 分钟不偏离；超出单个 task 边界或验证步骤含糊，偏离概率陡增。

反例来自冷启动实验：一个全新 agent 会话只读 SPEC + PLAN，被叮嘱"遇到不确定之处就暂停询问"，结果全程零暂停——Go 没装就自己 winget 装了 1.26.5（SPEC 要求 1.22+），make 没装就自己装了，测试文件归属也自行决定。这证明"自主而不偏离"的时长取决于 task 边界的确定性而非提示词叮嘱——这个发现直接呼应了本项目的核心命题。

## 四、最优 task 颗粒度

task 颗粒度本质是决策权的分配粒度。太细，TDD 沦为仪式；太粗，隐性决策权转移给 subagent（冷启动 agent 的 winget 安装就是拿走了本该属于人的环境决策权）。最优颗粒度 = "一个 subagent 一次会话内可完成 + 有客观验证步骤 + 不产生跨 task 的隐式决策"。PLAN 的"每步 2–5 分钟、明确文件路径、明确验证步骤"标准大体正确，但还缺一条：列出该 task 内禁止自行决策的事项。Go 版本事件后，我把环境约束写进 task 前置条件，偏离明显减少。

## 五、SPEC/PLAN 质量与"规约不清导致偏离"的具体案例

最典型的案例：PLAN 的 P0.5 预留了 `tests/demo/cold_start_test.go` 骨架，但 P0.1/P0.2 的验证步骤暗示需要独立测试，两处没有互相引用。冷启动 agent 据此建了 2 个独立测试文件、5 个独立测试函数，而我预期的结构是一个文件加 t.Run() 子测试。同时它用系统 Go 1.26.5 初始化 go.mod，而 SPEC §8.1 只写了"Go"没写版本号。

这不是"没写"，是"写了但没写精确"——最隐蔽的规约缺陷；若发生在正式实现阶段，修正成本是一整个 Phase 的返工。据此修订了 6 处 SPEC/PLAN（Go 版本、产出目录、测试归属、根目录名、make 依赖、风险清单）。我的体会：SPEC 的精确性比完整性更难做到，PLAN 的 task 边界约定比 SPEC 的功能描述更直接影响实现质量。

## 六、最有效的 prompt / context 策略

1. **跨模型外部审阅**：每 Phase 把代码变更与架构决策发给 ChatGPT 独立审阅，逐条裁决。为什么有效：ChatGPT 与主 agent 没有共享上下文，不存在"共同幻觉"，相当于一场盲审；它发现的 HITL 不可达、全量注入等问题，主 agent 自查永远不会报。
2. **Worktree 隔离**：每个 Phase 一个 worktree 一条分支，上下文互不污染，Phase 5/6 得以并行。
3. **结构化观察回灌**：FeedbackObservation 字段全部非导出，LLM 只能拿到 FormatForLLM() 的格式化结果——"告诉 agent 什么"是代码决策而非提示词决策。
4. **断言"系统没做什么"**：Demo 2 的治理拦截断言 ExecutionCount==0 而非 Decision==Deny。这条策略来自我给 agent 的一条 prompt："不要只验证返回了什么，要验证没发生什么。"

## 七、凭据与分发逼我想清楚的事

凭据：写业务代码永远不会想到——key 不能经 `export` 录入（会进 shell history），所以要做交互式隐藏输入；审计日志要自动脱敏（redactSensitiveFields 是我在审阅建议外独立补的：通用要求禁止凭据进日志，审计日志不脱敏就是合规漏洞）；环境变量回退的明文风险必须写进 README；进程列表可见性、DPAPI/Keychain 加密、清除命令，每一项都是"出事才知道"的问题。

分发："全新机器从零运行"的检验让我选择零依赖的 Go 单二进制，交叉编译 5 平台 + GitHub Releases + GHCR 镜像。但最大的教训在部署：腾讯云 SCF 部署花了四个多小时仍未完全打通，函数入口约定、域名与网络细节形成了远超预期的知识盲区。"最后一公里"可以比核心开发更耗时——这是本次项目最反直觉的一课。

## 八、如果重做会改变什么

1. Config 与 Credential 不留空包，Phase 3/4 就做完整，CLI 从第一天就用真实配置而非硬编码 MockProvider。
2. 部署提前到中期启动，先在本地用 Docker 完整验证全链路，而不是留到最后硬啃。
3. PR 流程更规范：worktree 分支多数直接 push，仅 1 个 PR，与"每个 worktree 对应一个 PR"的要求有差距。
4. GitTool 白名单补上对 --force 等危险 flag 的校验。
5. 给冷启动 agent 的不再是"暂停询问"的提示词，而是硬性约束机制——本项目已经证明：机制必须是代码，不能是提示词。

## 九、对 Superpowers 方法论的批判

它假设了五件事，其中三件在我的项目里不成立：

① **假设人类 reviewer 持续在场且有判断力**。40+ 条意见逐条裁决、11 次人工干预、每 Phase Gate Review——对单人项目接近全职的人力成本，"Human-Owned"的 Owned 实际是 Occupied。
② **假设提示词级约束有效**。冷启动实验直接证伪："暂停询问"不敌智能体惯性。讽刺的是，Superpowers 的技能本身就是提示词——它在"约束 agent"上与它想约束的 agent 有同样的软肋，这恰好是我项目命题的镜像：机制必须是代码。
③ **假设环境同质、工具链就绪**。make、Go 版本两个假设在 Windows 上第一小时就失效。
成立的两项：④ 测试运行成本必须低（Go 秒级跑完才撑得起红-绿节奏）；⑤ 项目可被预先线性分解——但我的治理从三层演进到五层，PLAN 修订 14 次才收敛，plan 必须持续修订而非一次写死。

我的总结判断：Superpowers 最大的价值不是流程，而是它强制留下的过程证据——SPEC / PLAN / AGENT_LOG / 冷启动记录，让"人在回路"有据可依；最大的风险是把流程仪式当成工程本身。课程的问题是"当 LLM 能完成大部分编码工作时，工程师的价值在哪里"。我的回答：用确定性代码封装不确定性（护栏、反馈、治理），给"做对了吗"提供客观验证，对一个系统从凭据到分发的全链路负责。CageHarness 这台"用 harness 造的 harness"，就是这份答案的可运行版本。
