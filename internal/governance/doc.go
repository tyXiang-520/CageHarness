// Package governance implements the ★ deep dimension: a 5-layer pipeline
// (Schema Validation → Risk Classification → Policy Engine → Execution Boundary
// → Execution Control → Decision → HITL → Audit Log).
// Every tool invocation must pass through Governance.
package governance