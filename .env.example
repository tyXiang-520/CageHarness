# OpenAI API Key
# 首次运行请执行 `harness init` 引导录入，或设置环境变量：
#   export OPENAI_API_KEY=sk-...
#
# 注意：.env 为明文文件，不建议作为安全存储方案。
# 推荐使用 OS Keychain（macOS Keychain / Windows Credential Manager / Linux Secret Service）。
OPENAI_API_KEY=